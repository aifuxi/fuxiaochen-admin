import { DEFAULT_PAGINATION_PARAMS } from '@/constants/unknown.ts';

import { GetTagsParams } from '@/features/tag/types.ts';

export const defaultParams: GetTagsParams = {
  ...DEFAULT_PAGINATION_PARAMS,
};
